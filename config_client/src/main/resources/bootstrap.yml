spring:
  cloud:
    config:
      name: config_client
      label: master
      discovery:
        enabled: true
        service-id: remote-config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/